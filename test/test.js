const expect = chai.expect;
const should = chai.should();

describe('bowlingScoreCalculator', function(){
  it('should be a function', function(){
    expect(bowlingScoreCalculator).to.be.a('function');
  })
})
